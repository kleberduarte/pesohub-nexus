DATABASE_URL="postgresql://pesohub:pesohub@localhost:5432/pesohub_nexus?schema=public"
# Dev local: REDIS_HOST/REDIS_PORT. Em produção (ex: Railway), prefira REDIS_URL
# (connection string única com credenciais) — tem prioridade se definida.
REDIS_HOST=localhost
REDIS_PORT=6379
# REDIS_URL=redis://default:senha@host:porta
